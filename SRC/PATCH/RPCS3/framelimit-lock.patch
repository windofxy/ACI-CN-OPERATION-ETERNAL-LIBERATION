diff --git a/rpcs3/rpcs3qt/settings_dialog.cpp b/rpcs3/rpcs3qt/settings_dialog.cpp
index 6194a85fd..9189f8ec9 100644
--- a/rpcs3/rpcs3qt/settings_dialog.cpp
+++ b/rpcs3/rpcs3qt/settings_dialog.cpp
@@ -503,6 +503,16 @@ settings_dialog::settings_dialog(std::shared_ptr<gui_settings> gui_settings, std
 		}
 	}
 
+	// OEL anticheat: frame limit locked to 30 (game logic is FPS-dependent)
+	for (int i = ui->frameLimitBox->count() - 1; i >= 0; i--)
+	{
+		const QVariantList var_list = ui->frameLimitBox->itemData(i).toList();
+		if (var_list.size() == 2 && static_cast<int>(frame_limit_type::_30) != var_list[1].toInt())
+		{
+			ui->frameLimitBox->removeItem(i);
+		}
+	}
+
 	m_emu_settings->EnhanceComboBox(ui->vsyncMode, emu_settings_type::VSync);
 	SubscribeTooltip(ui->vsyncMode, tooltips.settings.vsync);
 
